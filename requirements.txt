# Core app framework
streamlit>=1.38.0

# Data handling
pandas>=2.0.0

# LLM interaction (used for safe mode)
litellm>=1.47.0

# Giskard - LLM vulnerability scanner
giskard>=2.0.0

# Optional: needed if you want to run local models via litellm + ollama
# ollama>=0.3.0   # only if you plan to use ollama models

# Optional: for better error handling / debugging
# rich>=13.0.0
