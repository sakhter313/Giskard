streamlit  # Unpinned—lets Cloud use latest (1.52.2+)
transformers==4.45.2
datasets  # Unpinned—grabs latest 4.4.2
giskard[llm]==2.18.0  # Latest stable with LLM extras (scans, etc.)
requests==2.32.5  # Bumped to match Cloud's
pandas==2.3.3
huggingface_hub==0.23.6
